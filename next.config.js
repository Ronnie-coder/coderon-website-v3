/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you had any specific configurations, like reactStrictMode,
  // they would go here. For now, an empty object is perfect.
};

module.exports = nextConfig;